# 错题批改助手 v2.3 路线图

> 版本：v2.3（用户体系 + API Key 管理）
> 创建日期：2026-06-29
> 状态：开发中

---

## 1. 升级目标

v2.0~v2.2 完成了核心功能和体验打磨。v2.3 聚焦**用户体系完善与安全强化**，让每个用户拥有独立设置和自己的 API Key。

---

## 2. 功能拆解

### 2.1 用户认证修复

| 模块 | 状态 |
|------|------|
| 创建 SessionProvider 客户端组件 | ⏳ 待开发 |
| layout.tsx 全局包裹 SessionProvider | ⏳ 待开发 |
| 修复 Google OAuth 登录全流程 | ⏳ 待开发 |
| 登录态 UI（用户头像/退出） | ⏳ 待开发 |

当前 NextAuth Google OAuth 已配置但登录无法正常工作——根因是 layout 中缺少 `SessionProvider` 包裹，导致 `useSession()` 永远返回 `unauthenticated`。

### 2.2 API Key 加密管理

| 模块 | 状态 |
|------|------|
| AES-256-GCM 加密工具 `lib/encryption.ts` | ⏳ 待开发 |
| `user_settings` 表设计（加密 Key + 偏好） | ⏳ 待开发 |
| `POST /api/user/key` 加密存储用户 Key | ⏳ 待开发 |
| `GET /api/user/key` 返回脱敏状态（无明文） | ⏳ 待开发 |
| `DELETE /api/user/key` 删除用户 Key | ⏳ 待开发 |

**加密方案**：
- 使用 Node.js 内置 `crypto` 模块（零额外依赖）
- AES-256-GCM 加密，随机 IV，附加 authTag
- 主密钥来源于环境变量 `API_KEY_ENCRYPTION_SECRET`（fallback 到 `NEXTAUTH_SECRET`）
- 数据库中仅存储加密后的密文，永不明文存 Key

### 2.3 路由接入用户 Key

| 模块 | 状态 |
|------|------|
| `/api/correct` 优先使用用户 Key | ⏳ 待开发 |
| `/api/plans` 优先使用用户 Key | ⏳ 待开发 |
| `/api/quiz` 优先使用用户 Key | ⏳ 待开发 |
| 全局 Key fallback 机制 | ⏳ 待开发 |

**优先级**：用户个人 Key（已登录 + 已配置）> 环境变量 `ANTHROPIC_API_KEY`。

### 2.4 数据隔离

| 模块 | 状态 |
|------|------|
| `filterError` 查询过滤当前用户 | ⏳ 待开发 |
| 错题本只显示本人错题 | ⏳ 待开发 |
| 掌握度统计只统计本人数据 | ⏳ 待开发 |
| 学习计划/测验数据隔离 | ⏳ 待开发 |

### 2.5 用户中心页面

| 模块 | 状态 |
|------|------|
| `/settings` 页面 | ⏳ 待开发 |
| API Key 输入/更新/删除 UI | ⏳ 待开发 |
| 偏好设置（默认学科/模型） | ⏳ 待开发 |
| 使用统计 | ⏳ 待开发 |

---

## 3. 实施顺序

| 优先级 | 任务 | 预估 | 状态 |
|--------|------|------|------|
| P0 | 加密工具 + 用户设置表 | 0.5h | ⏳ |
| P0 | SessionProvider + 修复登录 | 0.5h | ⏳ |
| P0 | API Key CRUD 接口 | 0.5h | ⏳ |
| P1 | 路由改造（correct/plans/quiz） | 1h | ⏳ |
| P1 | 用户中心页面 | 1h | ⏳ |
| P2 | 数据隔离 | 0.5h | ⏳ |

---

## 4. 里程碑

| 阶段 | 交付 |
|------|------|
| Phase 1 | 登录可用 + API Key 加密存储 |
| Phase 2 | 路由使用用户 Key + 数据隔离 |
| Phase 3 | 用户中心页面完整可用 |
